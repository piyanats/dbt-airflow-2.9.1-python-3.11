# dbt packages
# Add dbt packages here to extend functionality
# Run `dbt deps` to install packages

packages:
  # dbt utils - commonly used macros and tests
  - package: dbt-labs/dbt_utils
    version: 1.1.1

  # Uncomment to add more packages
  # - package: dbt-labs/audit_helper
  #   version: 0.9.0

  # - package: dbt-labs/codegen
  #   version: 0.11.0
